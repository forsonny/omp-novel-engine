# Reference Manifest

This repository does not publish copied private uploads or third-party PDF/reference blobs.

Use `references/uploaded/` for local-only reference files that you own or have permission to use. The repository ignores those files by default and keeps only `references/uploaded/README.md`.

Public reference snapshots that are safe to keep with this repository live under `references/external/`.
